# Ralph Progress Log
Started: sáb 29 ago 2026 19:31:33 -03
---

## Codebase Patterns
- Stack: bash puro (bin/*.sh), seguindo o precedente do repo irmao /mnt/xs1000/me/BizOS/bin/setup.sh. Decisao explicitamente deixada em aberto no PRD ("open question parked, resolvida na hora de codar") e resolvida por precedente direto.
- Testes: bats (ja instalado em /usr/bin/bats). Padrao de teste: setup() cria WORKDIR via mktemp -d e cd nele; teardown() remove. Usar `run` do bats + assercoes com sentinela de conteudo (nao so status code) para evitar teste vacuous/fake-green.
- shellcheck INDISPONIVEL neste ambiente (sem sudo/apt-get, bloqueado por security policy). GATE 3 (typecheck) substituido por `bash -n <script>` — documentar essa substituicao em cada story que gerar/editar shell script.
- settings.json de portfolio gerado usa `"enabledPlugins": {}` (vazio) por padrao quando nenhum dominio e escolhido; cada dominio confirmado y/Y ganha uma chave `true` (wg-US-002).
- Mercado (BR/US/ambos) do portfolio vive em <slug>/portfolio.json, separado de .claude/settings.json (que e reservado a dominios/plugins habilitados).
- Padrao para prompts interativos em bin/*.sh sob `set -euo pipefail`: sempre `read -r -p "..." VAR || VAR=""` — sem o fallback, EOF de stdin (comum em testes bats/CI) mata o script por causa do set -e.
- O campo `mercado` em portfolio.json usa o enum canonico lowercase `br|us|ambos`; o setup aceita variacao apenas de caixa, rejeita sinonimos e valida antes de criar os arquivos do portfolio.
- MCP "integrado e funcional" (prd-update-1) = config declarativa: `<slug>/.mcp.json` + placeholder vazio no `<slug>/.env` (gitignorado). Testes assertam estrutura (jq/grep), nunca chamada de API. Sempre `rm -f "$SLUG/.mcp.json"` antes do if, para overwrite (ex. ambos→br) nao deixar MCP residual.
- Client HTTP first-party (brapi.dev, sem MCP): `bin/*.sh` com GET injetavel via env (`BRAPI_HTTP_GET`), fake nomeado em `tests/helpers/`, nunca chamada de API real nos testes. Cache por portfolio em `<slug>/_cache/` (gitignorado). Credencial so no `<slug>/.env`, nunca no stdout. Fallback yfinance e documentacao, nao client.
- Parser CVM (Informe Diario, CSV/ZIP): `bin/cvm-informe.sh` baixa `https://dados.cvm.gov.br/dados/FI/DOC/INF_DIARIO/DADOS/inf_diario_fi_YYYYMM.zip` via `CVM_HTTP_GET` (fake nomeado); `bin/cvm-informe-parse.py` filtra o CSV. Watchlist por portfolio em `<slug>/watchlist-fundos.json` (`{"cnpjs":[...]}`, CNPJ com ou sem pontuacao). Cache diario por dia civil UTC em `<slug>/_cache/cvm/` (nao TTL em segundos como brapi). Heredoc Python NAO pode consumir o stdin do CSV — parser em arquivo .py separado.
- Skills de research first-party (Claude Code): templates em `templates/skills/<name>/SKILL.md` (frontmatter `name` + `description: Use when...`); o setup copia para `<slug>/.claude/skills/` quando dominio `research` esta on, filtrado pelo enum de mercado (`br`→research-br, `us`→research-us, `ambos`→ambos). Sempre `rm -rf "$SLUG/.claude/skills"` antes de copiar, senao overwrite deixa skill residual. Testes assertam estrutura (frontmatter + needle da fonte: `brapi-quote.sh`/`cvm-informe.sh` vs `alpha-vantage`), nao invocam LLM nem API.
- Relatorio de alocacao (pr-US-001): `bin/alocacao.sh` + `bin/alocacao-report.py`. Posicoes manuais em `<slug>/holdings.json` (`{"posicoes":[{"ticker","quantidade","classe","mercado"}]}`); alvo em `<slug>/alocacao-alvo.json` (`porClasse` + `porMercado`, pesos somam 1). Cotacao BR via `brapi-quote.sh` (`regularMarketPrice`); US/global exige `ALOCACAO_QUOTE` injetado porque o MCP Alpha Vantage e so config declarativa. Fake nomeado: `tests/helpers/fake-alocacao-quote.sh`.
- Relatorio de risco (pr-US-002): `bin/risco.sh` + `bin/risco-report.py`. Historico BR acoes/ETFs/FIIs via `brapi-quote.sh` com `BRAPI_RANGE=3mo`/`BRAPI_INTERVAL=1d` (cache namespaced, nao colide com cotacao pontual); fundos com ticker CNPJ 14 digitos via `cvm-informe.sh` (`dtComptc`/`vlQuota`); US/global via `RISCO_HISTORY` injetado. VaR historico 95% interpolado (perda positiva), Sharpe rf=0 anualizado `*sqrt(252)`, `MIN_PRICES=3`. Ativo curto: aviso + `incluido:false`, metricas seguem; se ninguem entra, metricas `null` e exit 0. Fake nomeado: `tests/helpers/fake-risco-history.sh` / `fake-brapi-history.sh`.
- Rebalanceamento (pr-US-003): `bin/rebalanceamento.sh` chama `alocacao.sh` e `bin/rebalanceamento-report.py`. Threshold obrigatorio em `<slug>/alocacao-alvo.json` campo `threshold` fracao `(0, 1]` (sem default; parked do PRD). Dispara so se `abs(desvio) > threshold`. Sugestao por eixo `porClasse`/`porMercado` (visoes alternativas, nao blotter somavel): `vender` se desvio>0, `comprar` se desvio<0, `valor=abs(desvio)*total`. JSON sempre `executaOrdem: false`. Skill `templates/skills/rebalanceamento` copiada no setup quando dominio `risco` esta on.
- MCP de corretora (bi-US-001): mesmo acumulador `MCP_SERVERS` do Alpha Vantage; `rm -f` + rewrite. Dominio `corretora-banco` grava server `plaid` (`command: plaid-mcp`, `type: stdio`, `readOnly: true`) e placeholders `PLAID_CLIENT_ID=`/`PLAID_SECRET=` no `.env`. Products so leitura (`transactions` + `investments`); sem transfer/payment/order. `readOnly` e sentinela de teste — o lock real e o MCP escolhido + products. IBKR no catalogo e skill, nao MCP.
- Credencial de corretora (bi-US-002): `bin/credencial.sh <slug> has KEY` (allowlist `PLAID_CLIENT_ID`/`PLAID_SECRET`) le so `<slug>/.env` via grep/cut — nunca `source`, nunca env do processo, nunca imprime o valor. Placeholder vazio = ausente (exit 1). Config gerada continua com `${PLAID_*}` literal.
- Fallback de corretora (bi-US-003): `bin/holdings-sync.sh` decide pelo `has` (nunca `get`). Sem credencial = no-op no `holdings.json` (manual). Com credencial, fetch via seam `HOLDINGS_FETCH` (MCP Plaid e declarativo, sem client HTTP). Falha/token: `warn_stale` + exit 0, sidecar `<slug>/_cache/plaid/meta.json` `{fetchedAt}`, nunca grava `posicoes: []`. Fake nomeado: `tests/helpers/fake-plaid-holdings.sh`.
- Snapshot de NAV (fh-US-001): `bin/nav-snapshot.sh` delega a valorizacao integralmente a `bin/alocacao.sh`, extrai `.total` e faz upsert atomico por data local em `<slug>/nav-historico.json` (`[{data, valorTotal}]`) via temporario + `mv`; nunca duplica a data atual e preserva snapshots de outras datas.
- Log manual de transacoes (fh-US-002): `bin/transacao.sh <slug> registrar <tipo> <valor> [data]` faz append atomico em `<slug>/transacoes.json` (`[{data,tipo,valor}]`); tipo usa enum `aporte|resgate|compra|venda`, valor e numero positivo e data omitida usa o dia local.
- Benchmark BR (fh-US-003): `bin/benchmark-quote.sh <slug> br` delega a `bin/brapi-quote.sh` com ticker `^BVSP`, `BRAPI_RANGE=3mo` e `BRAPI_INTERVAL=1d`; token, TTL e cache historico `<slug>/_cache/brapi/^BVSP-3mo-1d.json` permanecem responsabilidade do cliente brapi existente.
- Benchmark US (fh-US-004): `bin/benchmark-quote.sh <slug> us` usa o override de processo `BENCHMARK_QUOTE` (mesmo argv `slug ticker mercado` de `RISCO_HISTORY`/`ALOCACAO_QUOTE`) com ticker fixo `^GSPC`. Sem a variavel: stderr + exit 1 no gap oficial; yfinance e so documentacao (usage/erro), nao client. Nao le `.mcp.json` nem `.env`. Fake nomeado: `tests/helpers/fake-benchmark-quote.sh`.
- Diagnostico sem historico (fh-US-005): `bin/diagnostico.sh` + `bin/diagnostico-report.py`. So snapshot de `holdings.json`. Concentracao = maior ticker por valor; exposicao cambial = `porMercado` br/us; liquidez = campo opcional `liquidez` (`D+0`/`D+1`, precedente `precoMedio`); DY 12m = `results[0].dividendYield` da brapi ou a string `indisponivel` (nunca inventa). Preco reusa `ALOCACAO_QUOTE`. Output JSON numerico, sem limite prescritivo. Fake DY: `BRAPI_DIVIDEND_YIELDS` no `fake-brapi-http.sh`.
- Relacao vs benchmark (fh-US-006): `bin/contra-benchmark.sh` + `bin/contra-benchmark-report.py`. Mercado de `portfolio.json` escolhe `^BVSP` (via `benchmark-quote.sh`) ou `^GSPC` (`BENCHMARK_QUOTE`); `ambos` emite `porMercado`. Retornos de `nav-historico.json` alinhados por intersecao de datas com o bench (mesmo periodo, sem interpolar). `MIN_ALIGNED=4` (3 retornos) porque 2 pontos de OLS deixam R²=1. Sem anualizar 252: snapshot de NAV nao e diario. Insuficiente (arquivo ausente, `[]`, ou poucos pontos) emite a string `historico insuficiente` nos 4 campos, exit 0.
- TWR/MWR (fh-US-007): `bin/retorno.sh` + `bin/retorno-report.py`. TWR encadeia `r_i = (NAV_i - CF_i)/NAV_{i-1} - 1` entre snapshots, CF = aporte - resgate em `(prev, curr]`; compra/venda internas ignoradas. MWR e IRR de holding period (nao anualizado, mesmo constraint do contra-benchmark) sobre fluxos de `transacoes.json` (aporte negativo, resgate positivo) + NAV final — sem NAV inicial extra (duplicaria o aporte de abertura). Sem `transacoes.json`/`[]` ou `len(NAV) < 2` emite a string `dado insuficiente` nos dois campos, exit 0.
- Eficiencia (fh-US-008): `bin/eficiencia.sh` + `bin/eficiencia-report.py`. Sortino = media dos retornos de NAV / downside (sqrt da media de min(r,0)^2, MAR=0, sem *sqrt(252)). Giro = soma `compra`+`venda` / media do NAV; aporte/resgate fora do numerador. Aliquota efetiva = soma `impostoPago` / soma `ganhoRealizado` (campos opcionais no JSON; CLI de `transacao.sh` nao expandida). Campos independentes: transacoes ausentes nao escondem Sortino. Sem registro de IR ou NAV < 2 emite `dado insuficiente` so no campo correspondente.

## 2026-08-29 - wg-US-001
- Implementado bin/setup.sh: cria <slug>/CLAUDE.md, <slug>/_memoria/, <slug>/.env (vazio), <slug>/.claude/settings.json (enabledPlugins vazio), <slug>/.gitignore (contendo .env). Idempotente via prompt y/N (read -r -p), sem flag de bypass; recusa/erro nao altera nada.
- Arquivos: bin/setup.sh (novo), tests/setup.bats (novo, 5 casos).
- Resolucao de ambiguidade de stack (Anti-Speculation Protocol): PRD (features/workspace-generator.html) marcava a escolha shell-script-vs-skill como "open question parked... decisao de implementacao, nao trava o PRD". Consultei o repo irmao BizOS (mesmo espirito citado na descricao do projeto) e segui o padrao ja estabelecido: bin/setup.sh em bash. Nao foi necessario bloquear a story.
- GATE 1: tests passing (5/5) | GATE 2: lint clean (sem lint aplicavel a bash alem de bash -n; sem achados do qa-reviewer) | GATE 3: typecheck clean (bash -n, substituto de shellcheck indisponivel) | GATE 4: coverage n/a (bash/bats nao tem tooling de coverage configurado neste projeto) | GATE 5: committed at 9c6b686 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
- Learnings: shellcheck nao pode ser instalado neste ambiente (sudo bloqueado por security policy). Usar bash -n como gate substituto em stories futuras de shell script, e documentar a substituicao no proprio commit/progress.
---

## 2026-08-29 - wg-US-002
- Implementado em bin/setup.sh: apos a checagem de sobrescrita, o script pergunta y/N para cada dominio (research, risco, dados-mercado, corretora-banco) e grava uma chave `true` por dominio confirmado em <slug>/.claude/settings.json.enabledPlugins (fica {} se nenhum for escolhido, preservando o AC de wg-US-001). Em seguida pergunta o mercado (BR/US/ambos) e grava em <slug>/portfolio.json (arquivo novo, distinto de settings.json).
- Arquivos: bin/setup.sh (editado), tests/setup.bats (3 casos novos: dominios habilitados, nenhum dominio habilitado, mercado registrado). .gitignore raiz criado (novo) so para ignorar .guarded-impl/ do guarded-impl-loop.
- Resolucao de ambiguidade (Anti-Speculation Protocol): o PRD nao definia (a) onde gravar o mercado ("config propria do portfolio", distinta de settings.json que ja e usado para dominios/plugins) nem (b) o shape de enabledPlugins quando pc-US-001 (catalogo de plugins real) ainda nao existe. Resolvido por leitura direta do texto do PRD (features/workspace-generator.html): "Out: instalacao de fato dos plugins escolhidos (feature Catalogo de Plugins)" deixa claro que so a INTENCAO por dominio precisa ser registrada, nao um id de plugin real — por isso um dict domain->true e suficiente. E "mercado... registrado em config propria do portfolio" (frase distinta da usada para dominios, que fala em settings.json) foi lido como pedido de arquivo proprio -> portfolio.json novo. Nao foi necessario bloquear a story.
- Gotcha: com `set -euo pipefail`, os `read -r -p` novos precisam de `|| resp=""` — sem isso, EOF de stdin (comum em testes bats/CI sem input piped) mata o script no meio por causa do set -e. Documentar esse padrao para qualquer prompt interativo futuro em bin/*.sh.
- GATE 1: tests passing (8/8) | GATE 2: lint clean | GATE 3: typecheck clean (bash -n) | GATE 4: coverage n/a | GATE 5: committed at 672812e | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine, provado por mutation testing manual nos 2 blocos novos; adherence: PASS via quati-revisor:qa-reviewer)
---

## 2026-08-29 21:03 - wg-US-003
- Implementado em bin/setup.sh: funcao slugify() (iconv translit + lowercase + sed hifenizando qualquer sequencia fora de [a-z0-9/-], colapsando hifens repetidos e removendo hifen nas pontas) roda sobre o argumento bruto ANTES de qualquer criacao de diretorio/arquivo. O resultado normalizado e validado contra ^[a-z0-9][a-z0-9/-]{0,78}[a-z0-9]$ mais checagem extra de "//" (".." e inalcancavel apos a normalizacao, pois "." nao sobrevive ao sed - ponto levantado pelo qa-reviewer como observacao nao-bloqueante, mantido por ser defensivo e barato). Rejeicao mostra o valor recebido original e o normalizado, alem do regex esperado.
- Arquivos: bin/setup.sh (editado), tests/setup.bats (3 casos novos: normalizacao com espacos/maiusculas, normalizacao com acentos via translit, rejeicao pos-normalizacao com mensagem explicativa).
- Resolucao de ambiguidade: nenhuma necessaria - AC era explicito sobre a normalizacao (slugify) e sobre a ordem (normaliza -> valida -> so rejeita se ainda violar).
- GATE 1: tests passing (11/11) | GATE 2: lint clean (bash -n, shellcheck indisponivel no ambiente - sudo bloqueado, mesmo substituto documentado em wg-US-001/002) | GATE 3: typecheck clean (bash -n) | GATE 4: coverage n/a (bash/bats sem tooling de coverage) | GATE 5: committed at 42122be | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine, provado por mutation testing manual na funcao slugify; adherence: PASS via quati-revisor:qa-reviewer)
---

## 2026-08-30 00:07 - wg-US-004
- Isolamento entre portfolios gerados lado a lado: nenhuma mudanca de producao necessaria em bin/setup.sh — a arquitetura per-slug ja estabelecida em wg-US-001/wg-US-002 (cada portfolio e um diretorio "$SLUG/" independente, sem estado global compartilhado) ja satisfaz os dois AC. Story implementada como 2 testes de regressao novos em tests/setup.bats que comprovam isso.
- Arquivos: tests/setup.bats (editado, 2 casos novos + reforco do primeiro apos achado do GATE 6).
- Resolucao de ambiguidade: nenhuma necessaria - os ACs eram claros e ja satisfeitos pela arquitetura existente. Documentado no proprio attempts.log que o "RED" desta story e atipico: os testes passaram de imediato (GREEN direto), pois nao ha bug/lacuna a corrigir - eles atuam como guarda de regressao contra futura introducao de estado global compartilhado.
- Achado do GATE 6 (qa-reviewer): o teste original do AC1 usava so asserções negativas (`[ ! -e "beta/.env" ] || [ ! -s "beta/.env" ]`) e passaria mesmo se beta/.env/beta/_memoria nunca fossem criados - cobertura ilusoria. Corrigido acrescentando asserções positivas (`[ -f beta/.env ]`, `[ -d beta/_memoria ]`) antes das negativas, tornando o teste genuinamente sensivel a mutacao (comprovado por TDR com mutation testing manual em bin/setup.sh, revertida).
- Debito registrado (fora do escopo desta story, achado LOW do qa-reviewer nao-bloqueante): bin/setup.sh:28 permite slug com "/" (ex. "acme/beta"), possibilitando portfolio aninhado dentro de outro - fisicamente isolado (sem compartilhar .env/_memoria/settings.json), mas o CLAUDE.md do pai passa a ser herdado como contexto ancestral pelo Claude Code. Decisao de produto pendente: e feature (portfolios hierarquicos) ou residuo a remover do regex? Nao afeta os ACs desta story.
- GATE 1: tests passing (13/13) | GATE 2: lint clean (bash -n, shellcheck indisponivel) | GATE 3: typecheck clean (bash -n) | GATE 4: coverage n/a | GATE 5: committed at 5b6edd1 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine via mutation testing manual; adherence: PASS via quati-revisor:qa-reviewer, 2 LOW findings - 1 corrigido nesta story, 1 registrado como debito de backlog acima)
---

## 2026-08-30 21:13 - pc-US-001
- Catalogo central de plugins por dominio: criado catalog.json na raiz do repo cobrindo os 4 dominios (research, risco-portfolio, dados-mercado, corretora-banco), cada entrada com nome/repo/tipo/credencial (boolean), catalogo curado a partir da tabela de research documentada em .ralph/investos/features/plugin-catalog.html. Entrada-gap explicita para dados de mercado BR (gap:true, credencial:false, disclaimer textual), sem "em breve" maquiado.
- Arquivos: catalog.json (novo), tests/catalog.bats (novo, 4 casos via jq: JSON valido, 4 dominios presentes, todo campo obrigatorio presente exceto repo em entrada-gap, gap BR explicito).
- Resolucao de ambiguidade: nenhum bloqueio necessario. O AC2 fala em "repo/link" por entrada, mas a entrada-gap por definicao nao tem repo real (documenta uma ausencia) - o teste 3 exclui entradas com gap:true da exigencia de repo, e o teste 4 garante que a entrada-gap continua declarando credencial:false e gap:true explicitamente (nao vira uma entrada normal disfarcada).
- AC3 "Typecheck passes": repo nao tem stack tipada (sem TypeScript/tsconfig) - substituto e `jq empty catalog.json`, documentado no proprio teste 1 e confirmado pelo qa-reviewer como equivalente disponivel.
- Achados do GATE 6 (qa-reviewer, ambos LOW, nao-bloqueantes, fora do escopo desta story): (1) as 3 entradas de research (`claude-trading-skills`, `quant_investing_skills`, `family-office`) tem `credencial:false` mas a tabela curada da spec as marca como "Variavel" - decisao de tri-estado fica para pc-US-002 (Entradas do catalogo declaram credencial exigida), que e o proximo da fila e bloqueado por esta story; (2) dominio risco-portfolio tem uma unica entrada reciclada de research (sem linha propria na tabela da spec) - cobertura nominal do AC1, mas qualidade de dado fraca. Registrar para consulta ao implementar pc-US-002/pc-US-003.
- GATE 1: tests passing (17/17, suite completa) | GATE 2: lint clean (sem lint aplicavel a JSON/bash alem de jq empty / bash -n) | GATE 3: typecheck clean (jq empty catalog.json, substituto documentado - repo sem stack tipada) | GATE 4: coverage n/a (bash/bats/JSON sem tooling de coverage) | GATE 5: committed at ff948eb | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine via mutation testing manual 4x; adherence: PASS via quati-revisor:qa-reviewer, 2 findings LOW nao-bloqueantes registrados acima para pc-US-002)

## 2026-08-30 - pc-US-002
- Entradas do catalogo declaram credencial exigida: mantido o campo `credencial` booleano (contrato ja testado por pc-US-001) e adicionado `disclaimer` explicito nas 4 entradas de credencial variavel (claude-trading-skills, quant_investing_skills em research, quant_investing_skills em risco-portfolio, family-office), documentando que a credencial efetiva depende da fonte de dados subjacente (ex. yfinance publico vs. provedor pago) - endereca o achado LOW herdado do GATE 6 de pc-US-001 (tabela curada da spec marca essas entradas como "Variavel").
- Arquivos: catalog.json (editado, 4 disclaimers novos), tests/catalog.bats (2 casos novos: caso 5 exige credencial:true em toda entrada nao-gap de dados-mercado/corretora-banco, cobrindo os exemplos do AC - Plaid/IBKR/Alpha Vantage; caso 6 exige disclaimer com "vari" nas 4 entradas de credencial variavel).
- Resolucao de ambiguidade (Anti-Speculation Protocol): o AC pede so marcacao binaria separada (sem credencial vs. com credencial) e a spec (Functional Requirements) fixa o campo credencial como "sim/nao" - por isso tri-estado no proprio campo foi descartado (quebraria o contrato boolean do caso 3 de pc-US-001 e a FR). A variabilidade das 3+1 entradas de research foi documentada via disclaimer em vez de um terceiro estado, seguindo o precedente ja existente do disclaimer em financial-services.
- Achado do GATE 6 (qa-reviewer, 5 LOW nao-bloqueantes, registrados como debito de backlog): (1) o campo `disclaimer` acumula 3 semanticas distintas (conector pago opcional, credencial variavel, gap) e fica ilegivel por maquina para o consumidor declarado (Gerador de Workspace) - sugestao: campo booleano irmao `credencialVariavel:true` ao lado de `credencial:false` tornaria o bucket "Variavel" machine-readable sem quebrar a FR nem o caso 3; (2) `tests/catalog.bats:45` usa numero magico `length == 4` que depende da duplicacao cross-dominio de quant_investing_skills - sugerido trocar para forma agnostica a contagem (`select(...|not)|length==0`, precedente do caso 3); (3) nome do teste diz "research" mas a query varre todos os dominios (inclui risco-portfolio); (4) `contains("vari")` e asserção frouxa sobre prosa livre; (5) disclaimer de 200+ chars duplicado 4x (JSON nao tem dedupe barato). Nenhum bloqueia o AC atual - registrados para consulta se uma story futura tocar o catalogo de credenciais novamente.
- GATE 1: tests passing (19/19, suite completa) | GATE 2: lint clean (jq empty / bash -n, sem lint aplicavel a JSON) | GATE 3: typecheck clean (jq empty catalog.json, substituto documentado) | GATE 4: coverage n/a (bash/bats/JSON sem tooling de coverage) | GATE 5: committed at 548f9f5 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine via mutation testing no caso 6, disclaimer removido/restaurado; adherence: PASS via quati-revisor:qa-reviewer, APPROVE WITH SUGGESTIONS, 0 CRITICAL/HIGH/MEDIUM, 5 LOW)
---

## 2026-08-30 - pc-US-003
- Gap de dados de mercado BR registrado explicitamente: o gap (entrada gap:true, credencial:false, disclaimer explicito, sem "em breve") ja existia em catalog.json desde pc-US-001 (ff948eb) - o AC1 estava satisfeito colateralmente, mas nenhum teste anterior travava a metade "visivel e nao maquiada" do AC (caso 4 de pc-US-001 so checava gap==true and credencial==false, sem olhar disclaimer/texto). Esta story converteu essa propriedade acidental em invariante testada: tests/catalog.bats caso 7 novo, via jq, exige disclaimer nao-vazio e sem "em breve" (case-insensitive) na entrada gap de dados-mercado.
- Arquivos: tests/catalog.bats (editado, +1 caso). catalog.json nao mudou (nao havia necessidade - mudar o dado que ja cumpre o AC seria scope creep).
- Resolucao de ambiguidade: nenhum bloqueio necessario. Precedente direto: wg-US-004 (mesmo padrao - AC ja satisfeito pela arquitetura/dado existente, story fecha com teste de regressao dedicado, RED atipico = GREEN direto, comprovado por mutation testing).
- RED atipico (mesmo padrao de wg-US-004): o teste passou de imediato ao ser escrito (GREEN direto), pois o dado ja cumpria o AC. Mutation testing manual confirmou sensibilidade genuina: disclaimer mutado para conter "em breve" -> teste falha (status != 0); restaurado -> volta a passar.
- Achado do GATE 6 (qa-reviewer, 1 LOW nao-bloqueante, registrado como debito de backlog): tests/catalog.bats:55 usa assert existencial (jq -e com select, passa se ALGUMA entrada gap satisfizer) em vez de universal (forma usada nos casos 3 e 5 do mesmo arquivo, com length==0). Hoje so ha 1 entrada gap em dados-mercado, entao o teste nao e decorativo (mutacao no unico caso real mata corretamente) - mas se uma segunda entrada gap for adicionada no futuro maquiada como "em breve", o teste atual continuaria verde. Sugestao registrada: trocar para forma universal (`select(negacao das condicoes) | length == 0`) se o catalogo ganhar mais de 1 entrada gap.
- GATE 1: tests passing (7/7, suite completa) | GATE 2: lint clean (jq/bash -n, sem lint aplicavel a JSON) | GATE 3: typecheck clean (jq empty catalog.json, substituto documentado) | GATE 4: coverage n/a (bash/bats/JSON sem tooling de coverage) | GATE 5: committed at d56854f | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine via mutation testing manual no caso 7; adherence: PASS via quati-revisor:qa-reviewer, APPROVE WITH SUGGESTIONS, 0 CRITICAL/HIGH/MEDIUM, 1 LOW)
---

## 2026-08-31 - mdr-US-001 - BLOCKED
- Ambiguidade: o AC pede "pelo menos 1 MCP do catalogo (ex.: Alpha Vantage) integrado e funcional quando dominio 'dados de mercado' + mercado US/global estao habilitados no setup" - duas decisoes arquiteturais reais faltam, e cada uma tem trade-offs materiais nao resolvidos pelo PRD:
  1. O campo "mercado" em portfolio.json (wg-US-002) e texto livre nao validado (usuario digita "BR"/"US"/"ambos"/qualquer string) - nao ha enum nem normalizacao. Detectar "mercado US/global habilitado" de forma confiavel exige decidir o enum aceito e se/como normalizar variacoes (ex. "usa", "estados unidos", "Us") ANTES desta story, e essa decisao nao esta no PRD nem foi tomada em nenhuma story anterior (wg-US-002 so testa que o valor digitado e gravado verbatim).
  2. "Integrado e funcional" e ambiguo quanto ao mecanismo: (a) so gravar config declarativa de MCP server (ex. $SLUG/.mcp.json com referencia ao Alpha Vantage MCP + variavel de credencial no .env, seguindo o padrao de wg-US-001) vs (b) implementar um client real que faz chamada de API contra Alpha Vantage. Sao trade-offs materialmente diferentes (testabilidade sem rede/credencial real vs. comportamento realmente "funcional" em runtime) e o PRD nao escolhe entre eles.
- Consultas tentadas (Resolution Budget, nenhuma resolveu):
  1. CLAUDE.md no diretorio-alvo e nos pais: inexistente em todo o repo (raiz, bin/) - nada a consultar.
  2. `ls .claude/skills/`: diretorio nao existe neste repo - nenhuma skill para consultar.
  3. Grep por precedente similar: nenhuma story anterior tocou integracao com API/MCP externo real ou validacao de enum de mercado; wg-US-002 so grava o texto digitado sem normalizar. Nao ha padrao equivalente no codebase para seguir.
- O que eu precisava saber: (a) enum/normalizacao definitiva para o campo "mercado" (valores aceitos, case-sensitivity, sinonimos) e onde essa validacao deveria morrer (dentro de wg-US-002 retroativamente, ou nova pre-condicao desta story); (b) se "integrado e funcional" para este projeto (gerador de workspace em bash, sem stack de app real, testes via bats+jq) significa config declarativa de MCP server (.mcp.json + credencial em .env) ou implementacao de client real com chamada de API.
- Recomendacao: re-rodar /prd para refinar mdr-US-001 (e possivelmente retroagir uma decisao de enum em wg-US-002) antes de re-rodar /ralph.
---

## 2026-08-29 21:43:26 -0300 - wg-US-005
- Implementado enum canonico de mercado no setup: entrada case-insensitive para `br`, `us` e `ambos`, persistencia lowercase e rejeicao de qualquer outro valor antes da criacao do portfolio, com mensagem contendo valor recebido e valores aceitos.
- Files changed: bin/setup.sh, tests/setup.bats, .ralph/investos/attempts.log.
- Learnings for future iterations:
  - Features consumidoras podem tratar portfolio.json.mercado como `br|us|ambos`, sem sinonimos nem variacao de caixa.
  - Testes antigos que executam o setup precisam fornecer um mercado valido explicitamente; EOF agora e entrada invalida pelo contrato refinado.
  - GATE 6 aprovou TDR genuino nos dois ACs comportamentais e aderencia PASS; sugestao LOW nao-bloqueante: numa manutencao futura, tornar a assercao da lista aceita independente da substring `br` presente em `brasil`.
- GATE 1: tests passing (22/22) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`) | GATE 4: coverage n/a (Bash/Bats sem tooling ou threshold) | GATE 5: committed at 8f466c5 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 22:06:01 -0300 - mdr-US-001
- Implementado em bin/setup.sh: quando dominio `dados-mercado` esta habilitado e mercado e `us` ou `ambos`, o setup grava `<slug>/.mcp.json` com o MCP Alpha Vantage (HTTP, URL com `${ALPHA_VANTAGE_API_KEY}`) e placeholder `ALPHA_VANTAGE_API_KEY=` em `<slug>/.env`. Mercado `br` ou dominio desligado nao gera MCP; `rm -f` do `.mcp.json` no overwrite evita residual.
- Files changed: bin/setup.sh, tests/setup.bats (2 casos novos), .ralph/investos/attempts.log.
- Learnings for future iterations:
  - "Integrado e funcional" nesta fase e so config declarativa (prd-update-1). Sem client HTTP, sem chamada de API nos testes.
  - Catalogo declara `https://www.alphavantage.co/mcp/` (pagina); o endpoint MCP HTTP usado no `.mcp.json` e `https://mcp.alphavantage.co/mcp?apikey=...`. URL hardcoded no setup, nao lida de catalog.json (nit LOW do reviewer — lookup dinamico nao e AC).
  - GATE 6: tdr-runner e qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato dos agents do quati-revisor (mesmo precedente das iteracoes anteriores). Mutation testing no orquestrador (3 mutacoes, restore) + TDR independente + aderencia PASS.
- GATE 1: tests passing (24/24) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats sem tooling ou threshold) | GATE 5: committed at 5b038ee | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 22:21:14 -0300 - mdr-US-002
- Implementado client first-party `bin/brapi-quote.sh`: cotacao BR via brapi.dev, cache em `<slug>/_cache/brapi/` (TTL 3600s, relogio injetavel via `BRAPI_NOW`), tickers livres PETR4/VALE3/MGLU3/ITUB4 sem token, demais exigem `BRAPI_TOKEN` no `<slug>/.env`. HTTP injetavel via `BRAPI_HTTP_GET` (fake nomeado nos testes). Setup grava placeholder + `_cache/` no gitignore quando dados-mercado + mercado br/ambos. Fallback yfinance `.SA` so documentado no usage.
- Files changed: bin/brapi-quote.sh (novo), bin/setup.sh, tests/brapi.bats (novo), tests/helpers/fake-brapi-http.sh (novo), tests/setup.bats, .ralph/investos/attempts.log.
- Resolution: read CLAUDE.md ausente; skills backend-testing (fake nomeado, sem rede) e security-best-practices (token fora de log/stdout); padrao bash de wg-US-001 + config .env de mdr-US-001. prd-update-1 (config declarativa) aplica so a MCP (mdr-US-001/bi-US-001); ACs desta story nao foram amendados — client proprio, testes mockam HTTP.
- Learnings for future iterations:
  - `BRAPI_HTTP_GET` + fake nomeado e o precedente de I/O HTTP neste repo (nao curl real em bats).
  - Token na query string e o contrato da brapi; stdout fica limpo, mas `ps` ainda ve a URL no curl de producao (LOW registrado).
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001). Mutation 4x (cache, free tickers, yfinance doc, token guard) todas killed; aderencia PASS com 6 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) TTL expirado nao tem teste apesar do seam `BRAPI_NOW`; (2) cache HIT nao avisa idade do dado (FR da feature, fora do AC); (3) yfinance so no usage(), descoberta fraca; (4) teste de mercado `us` nao afirma ausencia de `BRAPI_TOKEN`; (5) token visivel em `ps` via query string; (6) falha HTTP nao tem caso de teste. Nenhum e X→Z.
- GATE 1: tests passing (32/32) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats sem tooling ou threshold) | GATE 5: committed at 2d67d5e | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 22:40:00 -0300 - mdr-US-003
- Implementado parser first-party do Informe Diario CVM: `bin/cvm-informe.sh` baixa o ZIP mensal em dados.cvm.gov.br, extrai o CSV e `bin/cvm-informe-parse.py` devolve JSON com cota/PL/captacao/resgate. Watchlist por portfolio em `<slug>/watchlist-fundos.json`. Cache do ZIP por dia civil UTC (batch diario republica M e M-1). HTTP injetavel via `CVM_HTTP_GET`; testes usam fake nomeado, sem rede.
- Files changed: bin/cvm-informe.sh (novo), bin/cvm-informe-parse.py (novo), tests/cvm-informe.bats (novo, 8 casos), tests/helpers/fake-cvm-http.sh (novo), .gitignore (`__pycache__/`/`*.pyc`), .ralph/investos/attempts.log.
- Resolution: read CLAUDE.md ausente; skills backend-testing (fake nomeado, sem rede) e precedente brapi (`BRAPI_HTTP_GET` + `_cache/`). Open question parked da feature (estrutura da watchlist) resolvida ao codar: arquivo por portfolio `watchlist-fundos.json` com `{"cnpjs":[...]}`.
- Learnings for future iterations:
  - `python3 - <<'PY'` come o stdin do pipe — parser CSV precisa ser arquivo .py (argv = watchlist, stdin = CSV).
  - Cache CVM e por dia civil, nao TTL de 3600s: o ZIP do mes corrente e reapresentado todo dia util.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001/002). Mutation 4x (filtro, dia seguinte, vlQuota, host CVM) todas killed; TDR independente concordou; aderencia PASS com 2 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) cota/PL/captacao/resgate viram `float` (precisao IEEE-754 vs decimal CVM); (2) `write_fetch_meta` antes do `unzip` pinaria ZIP ruim ate o dia seguinte. Nenhum e X→Z.
- GATE 1: tests passing (40/40) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 758bd9d | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 22:55:00 -0300 - mdr-US-004
- Implementado 1 skill de research (fundamentals) por mercado habilitado: templates first-party `templates/skills/research-br/SKILL.md` (brapi-quote.sh + cvm-informe.sh) e `templates/skills/research-us/SKILL.md` (MCP alpha-vantage). `bin/setup.sh` copia para `<slug>/.claude/skills/` quando dominio `research` esta on, filtrado por mercado; `rm -rf` no overwrite evita residual.
- Files changed: bin/setup.sh, templates/skills/research-br/SKILL.md (novo), templates/skills/research-us/SKILL.md (novo), tests/setup.bats (5 casos), .ralph/investos/attempts.log.
- Resolution: read CLAUDE.md ausente; `.claude/skills/` do repo inexistente; precedente BizOS (`cliente-piloto/.claude/skills/*/SKILL.md`) + skill-authoring-guide (frontmatter name/description "Use when") + prd-update-1 (testes de estrutura, sem API/LLM). "Funcional" = SKILL.md valido wired as fontes ja entregues em mdr-US-001/002/003. Tipo escolhido: fundamentals (AC pede >=1; screener/comps/DCF ficam de fora). Gate de dominio: `research` (FR4), nao `dados-mercado`.
- Learnings for future iterations:
  - Copiar skill de template relativo a `BASH_SOURCE` (`REPO_ROOT/templates/skills`), nunca ao cwd — testes bats rodam em mktemp.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001/002/003). Mutation 5x (cp BR, cp US, rm -rf, needle brapi, fundamentals) todas killed; aderencia PASS com 2 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) teste research+US nao co-habilita dados-mercado, entao nao regressa `.mcp.json` junto da skill; (2) falta overwrite research-on→off (o `rm -rf` existe, o caso bats nao). Nenhum e X→Z.
- GATE 1: tests passing (45/45) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 56ffe23 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 23:20:00 -0300 - pr-US-001
- Implementado relatorio de alocacao atual vs. alvo: `bin/alocacao.sh` valoriza posicoes de `<slug>/holdings.json` (BR via `brapi-quote.sh`, US via `ALOCACAO_QUOTE` injetado) e `bin/alocacao-report.py` emite JSON `{total, porClasse, porMercado}` com atual/alvo/desvio contra `<slug>/alocacao-alvo.json`. Sem corretora: so holdings.json manual.
- Files changed: bin/alocacao.sh (novo), bin/alocacao-report.py (novo), tests/alocacao.bats (novo, 7 casos), tests/helpers/fake-alocacao-quote.sh (novo), .ralph/investos/attempts.log.
- Resolution: CLAUDE.md ausente; skills backend-testing (fake nomeado, bats, sem rede) e precedente `cvm-informe` (bash CLI + python da conta). Schema de holdings/alvo decidido ao codar (mesmo padrao da watchlist). Cotacao US nao ganhou client HTTP — mdr-US-001 e config declarativa; seam `ALOCACAO_QUOTE` cobre testes mistos e producao US falha com mensagem recebido/esperado.
- Learnings for future iterations:
  - Preco BR = `.results[0].regularMarketPrice` do payload brapi. Pesos do alvo precisam somar 1 (tolerancia 1e-6).
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001..004). Mutation 2x (slice_report atual=alvo; HOLDINGS path) ambas killed; aderencia PASS com 2 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) soma qty*preco mistura BRL/USD sem FX — AC nao pede cambio; (2) jq itera `.posicoes[]` antes da validacao Python, entao JSON malformado cai em stderr do jq em vez da mensagem canonica. Nenhum e X→Z.
- GATE 1: tests passing (52/52) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 6cacbba | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 23:55:00 -0300 - pr-US-002
- Implementado relatorio de risco: `bin/risco.sh` coleta historico por posicao de `<slug>/holdings.json` e `bin/risco-report.py` emite JSON com `varHistorico` (95% interpolado, perda positiva), `sharpe` (rf=0, *sqrt(252)), `maxDrawdown`, `avisos` e `ativos`. BR acoes via brapi `range=3mo`; fundos CNPJ via CVM; US via `RISCO_HISTORY` injetado. Ativo curto: aviso + `incluido:false`, metricas seguem; carteira vazia: metricas `null`, exit 0.
- Files changed: bin/risco.sh (novo), bin/risco-report.py (novo), bin/brapi-quote.sh (BRAPI_RANGE/cache namespaced), tests/risco.bats (novo, 6 casos), tests/helpers/fake-risco-history.sh (novo), tests/helpers/fake-brapi-history.sh (novo), .ralph/investos/attempts.log.
- Resolution: CLAUDE.md ausente; skills backend-testing (fake nomeado, bats, sem rede) e precedente pr-US-001 (inject US porque MCP e declarativa). Confianca/rf/MIN_PRICES decididos ao codar (PRD nao fixou): VaR 95%, rf=0, 3 precos. Fundo BR identificado por ticker CNPJ 14 digitos, sem schema novo em holdings.json.
- Learnings for future iterations:
  - Historico brapi nao pode reusar o cache da cotacao pontual — `BRAPI_RANGE` muda URL e o arquivo de cache (`TICKER-3mo-1d.json`).
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001..004 / pr-US-001). Mutation 4x (MIN_PRICES, BRAPI_RANGE, is_cnpj/CVM, historical_var=0) todas killed; aderencia PASS com 2 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) serie com >=3 precos e retorno constante (`stdev=0`) aborta o relatorio via `die` em vez de degradar Sharpe; (2) intersecao curta de calendarios anula metricas e nao desmarca `incluido`. Nenhum e X→Z.
- GATE 1: tests passing (58/58) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at be368bb | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-29 23:55:00 -0300 - pr-US-003
- Implementado sugestao de rebalanceamento: `bin/rebalanceamento.sh` valoriza via `alocacao.sh` e `bin/rebalanceamento-report.py` emite JSON `{threshold, disparou, executaOrdem:false, sugestoes[]}`. Threshold obrigatorio em `<slug>/alocacao-alvo.json`. Skill `templates/skills/rebalanceamento` copiada no setup quando dominio `risco` esta on. Nunca grava holdings nem envia ordem.
- Files changed: bin/rebalanceamento.sh (novo), bin/rebalanceamento-report.py (novo), bin/setup.sh, templates/skills/rebalanceamento/SKILL.md (novo), tests/rebalanceamento.bats (novo, 7 casos), .ralph/investos/attempts.log.
- Resolution: CLAUDE.md ausente; skills backend-testing (fake nomeado, bats) e skill-authoring-guide (frontmatter Use when); precedente pr-US-001 (CLI + python report) e mdr-US-004 (skill via setup). Open question parked (default 5%) resolvida ao codar: sem default, campo obrigatorio.
- Learnings for future iterations:
  - "Ultrapassa" = `abs(desvio) > threshold` (igual nao dispara). Eixos classe e mercado sao visoes alternativas, nao blotter somavel.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001..004 / pr-US-001/002). Mutation 4x (<=, comprar/vender, executaOrdem, cp setup) todas killed; TDR independente concordou; aderencia PASS com 1 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): lista plana `porClasse`+`porMercado` pode ser lida como ordens a somar e dobrar notional. Cada item esta certo no eixo; o conjunto nao e blotter executavel. Nenhum e X→Z.
- GATE 1: tests passing (65/65) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 71108fe | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 00:15:00 -0300 - bi-US-001
- Implementado MCP Plaid read-only via config declarativa: `bin/setup.sh` acumula `MCP_SERVERS` (Alpha Vantage + Plaid) e grava `<slug>/.mcp.json` so se houver server. Dominio `corretora-banco` adiciona `plaid-mcp` (stdio, `readOnly: true`, products `transactions`/`investments`) e placeholders `PLAID_CLIENT_ID=`/`PLAID_SECRET=` no `.env`. Overwrite `rm -f` evita residual. IBKR parked (skill no catalogo, nao MCP).
- Files changed: bin/setup.sh, tests/setup.bats (5 casos novos + isolamento wg-US-004 deixa de exigir .env vazio quando beta tem corretora), .ralph/investos/attempts.log.
- Resolution: CLAUDE.md ausente; skills backend-testing (estrutura, sem rede) e security-best-practices (credencial so no .env, `${VAR}` no config); precedente mdr-US-001 (config declarativa, prd-update-1). Provedor: Plaid (parked do PRD nao trava; catalogo tem MCP Plaid, IBKR e skill).
- Learnings for future iterations:
  - Dois MCPs no mesmo portfolio exigem acumulador `MCP_SERVERS`, nao `jq -n > .mcp.json` que sobrescreve.
  - `readOnly: true` e sentinela; o lock real e `command: plaid-mcp` + products sem transfer/payment/order (LOW do reviewer).
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001..004 / pr-US-001..003). Mutation 4x (bloco plaid, readOnly, transfer, merge) todas killed; aderencia PASS com 2 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) `readOnly` nao e schema do Claude Code — se trocar o MCP por um com ordem, a flag nao bloqueia write; (2) overwrite coberto so no caminho corretora on→tudo off, falta o par dados-mercado US on + corretora off. Nenhum e X→Z.
- GATE 1: tests passing (70/70) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at a6b9ae5 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 00:32:00 -0300 - bi-US-002
- Implementado reader first-party `bin/credencial.sh <slug> has KEY`: le `PLAID_CLIENT_ID`/`PLAID_SECRET` so de `<slug>/.env` (grep/cut, nunca source, nunca env do processo) e nunca imprime o valor. Placeholder vazio = ausente. Config do setup continua com `${PLAID_*}` literal mesmo depois do usuario preencher o `.env`.
- Files changed: bin/credencial.sh (novo), tests/credencial.bats (novo, 9 casos), .ralph/investos/attempts.log.
- Resolution: read CLAUDE.md ausente; skills backend-testing (bats, sem rede) e security-best-practices (nao source .env, credencial fora de stdout/config); precedente `brapi-quote.sh:read_token` + placeholders de bi-US-001. Sem client HTTP Plaid — bi-US-001 e config declarativa; esta story fecha o isolamento da leitura.
- Learnings for future iterations:
  - `has` devolve so exit 0/1. Nao existe `get` que imprime o valor — bi-US-003 deve reusar o parse, nao um CLI que vaza.
  - Testes de "ausente" precisam de `status -eq 1`, nao `-ne 0`, senao exit 127 (binario ausente) fica fake-green no RED.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001..004 / pr-US-001..003 / bi-US-001). Mutation 4x (process-env, print-value, source-env, empty-ok) todas killed; TDR independente reconfirmou h1; aderencia PASS com 1 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): slug inexistente cai no mesmo exit 1 de credencial ausente, diferente do `Portfolio invalido` de `brapi-quote.sh`. Nenhum e X→Z.
- GATE 1: tests passing (79/79) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 36d108a | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 - bi-US-003
- Implementado `bin/holdings-sync.sh`: sem PLAID_CLIENT_ID+PLAID_SECRET (via `credencial.sh has`) nao busca e nao altera holdings.json; com credencial, fetch via `HOLDINGS_FETCH`. Falha/token: aviso de idade + exit 0, mantem o ultimo arquivo, nunca grava posicoes vazias. alocacao/rebalanceamento seguem no arquivo manual.
- Files changed: bin/holdings-sync.sh (novo), tests/holdings-sync.bats (novo, 4 casos), tests/helpers/fake-plaid-holdings.sh (novo), .ralph/investos/attempts.log.
- Resolution: read CLAUDE.md ausente; skills backend-testing (fake nomeado, bats, sem rede) e security-best-practices (nunca `get`/print de secret); precedente ALOCACAO_QUOTE/RISCO_HISTORY + credencial.sh `has`. MCP Plaid continua declarativo — sem client REST (access_token nao esta no PRD).
- Learnings for future iterations:
  - Desconectar = placeholder vazio. O holdings.json no disco ja e o fallback (FR3: sem formato paralelo).
  - Idade mora em sidecar `_cache/plaid/meta.json` `{fetchedAt}`, nao no schema de posicoes.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de mdr-US-001..004 / pr-US-001..003 / bi-US-001/002). Mutation 4x (credential-gate, zero-on-fail, idade, exit-1) todas killed; TDR independente reconfirmou; aderencia PASS com 3 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) `fetch_holdings` emite erro de HOLDINGS_FETCH vazio em stdout, nao stderr; (2) bats nao tem caso visivel de `{"posicoes":[]}` (o ramo `length > 0` existe); (3) `risco.sh` nao e exercitado no teste de "demais features" (so alocacao+rebalanceamento). Nenhum e X→Z.
- GATE 1: tests passing (83/83) | GATE 2: lint clean (`bash -n`, shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n` / `python3 -m py_compile` / `jq empty catalog.json`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 0327014 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 19:42 - fh-US-001
- What was implemented: `bin/nav-snapshot.sh <slug>` reutiliza `bin/alocacao.sh`, extrai o valor total e grava o snapshot do dia em `nav-historico.json`; nova execucao no mesmo dia substitui o valor anterior sem duplicar, com escrita atomica.
- Files changed: `bin/nav-snapshot.sh`, `tests/nav-snapshot.bats`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: lidos `CLAUDE.md`, `features/historico-metricas.html` e o precedente `bin/alocacao.sh`; a dependencia de alocacao definida no PRD resolveu o mecanismo sem duplicar a logica de valorizacao. `.claude/skills/` nao existe neste repo; usado `quati-revisor:guarded-impl-loop` no GATE 6. Cursor Bridge `explore`/`read_slice` falhou nesta sessao com `Reading additional input from stdin`; leitura focal seguiu por context-mode.
- Learnings for future iterations:
  - `nav-historico.json` e um array de `{data, valorTotal}` e o snapshot diario deve preservar outras datas, removendo apenas a entrada da data atual antes de acrescentar o valor mais recente.
  - Reutilizar a saida `.total` de `alocacao.sh` mantem a mesma seam `ALOCACAO_QUOTE` e evita uma segunda implementacao de precificacao.
  - GATE 6: TDR `validated=yes mechanism=genuine` nos dois ACs comportamentais; aderencia PASS. Sugestao LOW nao-bloqueante: adicionar futuramente um teste explicito que semeie data anterior e prove sua preservacao.
- GATE 1: tests passing (108/108) | GATE 2: lint clean (n/a; nenhum linter configurado) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (sem tooling ou threshold) | GATE 5: committed at 11bb796 | GATE 6: guarded-impl-loop PASS
---

## 2026-08-30 20:02 - fh-US-002
- What was implemented: `bin/transacao.sh <slug> registrar <tipo> <valor> [data]` registra aportes, resgates, compras e vendas em `transacoes.json`, preservando registros anteriores com escrita atomica; data omitida usa hoje e o valor fica numerico no JSON.
- Files changed: `bin/transacao.sh`, `tests/transacao.bats`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: usado `ivt-quality:backend-testing`; `.claude/skills/` e CLAUDE.md nao existem neste repo. Seguidos precedentes de validacao em `aporte.sh`, escrita atomica em `nav-snapshot.sh` e testes Bats existentes. Cursor Bridge mapeou precedentes e executou fases RED/GREEN; `quati-revisor:guarded-impl-loop` revisou em `review-first`.
- Learnings for future iterations:
  - `transacoes.json` e array append-only de `{data,tipo,valor}`; tipos canonicos sao `aporte|resgate|compra|venda` e valor e numero positivo.
  - TDRs provaram mecanismo genuino em 2 hipoteses (persistencia/data/tipos e rejeicoes); aderencia PASS.
  - Achados LOW nao-bloqueantes: data explicitamente vazia nao cai no default; guard de JSON corrompido nao tem teste; reviewer sugeriu comentario-cabecalho e futura extracao do bloco atomico. Nenhum altera AC atual.
- GATE 1: tests passing (112/112) | GATE 2: lint clean (`git diff --check`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (sem tooling ou threshold) | GATE 5: committed at 672eb42 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 20:17:44 -0300 - fh-US-003
- What was implemented: `bin/benchmark-quote.sh <slug> br` consulta o historico do Ibovespa ao delegar para `bin/brapi-quote.sh` com ticker `^BVSP`, range `3mo` e intervalo `1d`; preserva o mecanismo existente de token, TTL e cache e nao faz rede nos testes.
- Files changed: `bin/benchmark-quote.sh`, `tests/benchmark.bats`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: lidos `CLAUDE.md`, `ivt-quality:backend-testing` e precedentes `bin/brapi-quote.sh`, `bin/risco.sh`, `tests/brapi.bats`, `tests/risco.bats`; `.claude/skills/` nao existe. Cursor Bridge mapeou precedentes e executou RED/GREEN; `quati-revisor:guarded-impl-loop` revisou em `review-first`.
- Learnings for future iterations:
  - Benchmark BR e um wrapper fino; cache/token continuam centralizados em `brapi-quote.sh`, evitando segundo cliente HTTP.
  - RED: 4 testes falharam com o script ausente; GREEN e suite completa passaram sem rede real.
  - GATE 6: TDR `validated=yes mechanism=genuine`; aderencia PASS. Dois LOW nao-bloqueantes ficam para backlog: argumento `BR` maiusculo nao e normalizado e o endpoint real com `^BVSP` cru nao foi provado contra a forma `%5EBVSP` do spike. O segundo nao foi alterado sem evidencia de rede, conforme anti-especulacao.
- GATE 1: tests passing (116/116) | GATE 2: lint clean (`bash -n`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (sem tooling ou threshold) | GATE 5: committed at 0685d37 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 20:42:00 -0300 - fh-US-004
- What was implemented: `bin/benchmark-quote.sh <slug> us` consulta o S&P 500 via `BENCHMARK_QUOTE` injetado (`^GSPC`); sem o override, erro de gap oficial (yfinance so documentado, nao-oficial, sem quota). Feat ja estava em 8474b79 da iteracao anterior; esta iteracao fechou GATE 6 e marcou a story.
- Files changed: `bin/benchmark-quote.sh`, `tests/benchmark.bats`, `tests/helpers/fake-benchmark-quote.sh`, `CLAUDE.md` (no feat 8474b79); `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`, `.ralph/investos/attempts.log` (nesta iteracao).
- Resolution: lidos `CLAUDE.md`, `ivt-quality:backend-testing`, precedente `RISCO_HISTORY`/`ALOCACAO_QUOTE`, spike `benchmark-nav.md` (H2 premium refutada, H3 yfinance fallback). `.claude/skills/` nao existe neste repo. GATE 6 via `quati-revisor:guarded-impl-loop` review-first (tdr-runner/qa-reviewer nao resolvem como subagent_type neste harness Grok; despacho general-purpose com o contrato).
- Learnings for future iterations:
  - US de indice nao usa Alpha Vantage MCP: `INDEX_DATA` e premium; a costura e env, igual as outras fontes US.
  - RED desta story foi na iteracao anterior (4 testes, script ainda sem ramo us); GREEN ja commitado em 8474b79 antes do GATE 6.
  - GATE 6: TDR `validated=yes mechanism=genuine` (AC1 inject ^GSPC; AC2/AC3 gap oficial + nao-MCP, 0 survivors); aderencia PASS via qa-reviewer (APROVA, 0 findings).
- GATE 1: tests passing (118/118) | GATE 2: lint clean (`bash -n`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 8474b79 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 21:15:00 -0300 - fh-US-005
- What was implemented: `bin/diagnostico.sh` + `bin/diagnostico-report.py` reportam concentracao no maior ativo, % por mercado br/us, % em liquidez D+0/D+1 (campo opcional `liquidez` em holdings.json) e DY 12m via brapi `dividendYield` (senao a string `indisponivel`). Output JSON sem limite prescritivo.
- Files changed: `bin/diagnostico.sh`, `bin/diagnostico-report.py`, `tests/diagnostico.bats`, `tests/helpers/fake-brapi-http.sh`, `CLAUDE.md`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: lidos `CLAUDE.md`, `ivt-quality:backend-testing`, precedente `bin/alocacao.sh` + campo opcional `precoMedio` em `perdas.sh`. `.claude/skills/` nao existe neste repo. Open question parked (log de dividendos em transacoes.json) resolvida pelo AC: DY vem da brapi, nao de transacoes. Liquidez nao mapeia classe→D+0/D+1 (inventaria regra de mercado).
- Learnings for future iterations:
  - Campo opcional em holdings (como `precoMedio`) e o precedente pra dado declarado pelo usuario sem quebrar o schema exigido por alocacao/risco.
  - DY US fica `indisponivel` de proposito: a fonte do AC e a brapi, nao o MCP Alpha Vantage.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de fh-US-004). Mutation 4x (concentracao hardcoded, liquidez zerada, DY inventado, texto prescritivo) todas killed; aderencia PASS com 2 LOW.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) teste de DY numerico so no caminho BR-only (sem `ALOCACAO_QUOTE`); o fallback `brapi_dividend_yield` com carteira mista nao e asserido com DY presente; (2) se `ALOCACAO_QUOTE` passar `dividendYield`, US deixaria de ser forçado a `indisponivel`. Nenhum e X→Z.
- GATE 1: tests passing (126/126) | GATE 2: lint clean (`bash -n`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 77bc00a | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 21:20:00 -0300 - fh-US-006
- What was implemented: `bin/contra-benchmark.sh` + `bin/contra-benchmark-report.py` calculam Beta, Alfa, R-quadrado e Tracking Error dos retornos de `nav-historico.json` contra o benchmark equivalente (`^BVSP` / `^GSPC`) no mesmo periodo (intersecao de datas). Com 2 observacoes ou arquivo ausente, os quatro campos saem como a string `historico insuficiente`.
- Files changed: `bin/contra-benchmark.sh`, `bin/contra-benchmark-report.py`, `tests/contra-benchmark.bats`, `CLAUDE.md`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: lidos `CLAUDE.md`, `ivt-quality:backend-testing`, precedente `bin/risco.sh` (bash+python, MIN_PRICES) e `bin/benchmark-quote.sh`. `.claude/skills/` nao existe neste repo. Mercado equivalente lido de `portfolio.json`; `ambos` emite `porMercado`. Sem anualizar 252 (NAV nao e serie diaria). `MIN_ALIGNED=4` porque OLS de 2 pontos deixa R²=1.
- Learnings for future iterations:
  - Alinhar NAV esparso com bench diario e inner-join de datas, nao interpolar nem usar 252.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de fh-US-005). Mutar so `MIN_ALIGNED` nao mata o teste de 2 obs (ainda cai em `len(returns)<2`); TDR genuino exige derrubar os dois guards.
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) sem teste de `mercado=ambos`; (2) fronteira de 3 NAVs alinhados (R² tautologico) sem teste dedicado; (3) `nav-historico.json` presente mas 0 bytes cai em JSONDecodeError em vez da string. Nenhum e X→Z.
- GATE 1: tests passing (132/132) | GATE 2: lint clean (`bash -n`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 1381c25 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 21:35:00 -0300 - fh-US-007
- What was implemented: `bin/retorno.sh` + `bin/retorno-report.py` calculam TWR (cadeia geometrica dos sub-periodos entre snapshots de `nav-historico.json`, isolando aporte/resgate) e MWR (IRR de holding period dos fluxos de `transacoes.json` + valor final). Sem transacoes ou NAV < 2 snapshots emite a string `dado insuficiente`.
- Files changed: `bin/retorno.sh`, `bin/retorno-report.py`, `tests/retorno.bats`, `CLAUDE.md`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: lidos `CLAUDE.md`, `ivt-quality:backend-testing`, precedente `bin/contra-benchmark.sh` (bash+python, string de insuficiente, sem anualizar). `.claude/skills/` nao existe neste repo. Compra/venda internas fora do CF. MWR nao injeta NAV inicial (AC lista transacoes + valor final).
- Learnings for future iterations:
  - TWR isola CF no intervalo `(prev, curr]`; aporte no mesmo dia do primeiro snapshot ja esta no NAV e nao entra de novo.
  - Mutar so `MIN_NAV` nao mata os testes de `dado insuficiente` — `holding_period_irr` ainda devolve None. TDR genuino do AC3 exige mutar `insufficient_payload` para numero.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de fh-US-006).
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) frase-mae do CLAUDE.md ainda cita `holdings.json` como input generico dos reports; (2) se Newton/IRR falha por motivo distinto de AC3, TWR tambem vira a string (relatorio atomico, alinhado aos testes). Nenhum e X→Z.
- GATE 1: tests passing (139/139) | GATE 2: lint clean (`bash -n`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at f12fa9f | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---

## 2026-08-30 21:46:00 -0300 - fh-US-008
- What was implemented: `bin/eficiencia.sh` + `bin/eficiencia-report.py` calculam Sortino (retornos de `nav-historico.json`, so desvio negativo, MAR=0, sem anualizar), giro (`compra`+`venda` / media do NAV) e aliquota efetiva de IR (`impostoPago` / `ganhoRealizado`, campos opcionais). Sem transacoes ou campos de IR, o campo correspondente emite `dado insuficiente`.
- Files changed: `bin/eficiencia.sh`, `bin/eficiencia-report.py`, `tests/eficiencia.bats`, `CLAUDE.md`, `.ralph/investos/attempts.log`, `.ralph/investos/prd.json`, `.ralph/investos/progress.txt`.
- Resolution: lidos `CLAUDE.md`, `ivt-quality:backend-testing`, precedente `bin/retorno.sh` (bash+python, string de insuficiente, campos independentes so onde o AC pede). Parked de `historico-metricas.html` (formato de imposto/ganho) resolvido ao codar: `impostoPago`/`ganhoRealizado`, CLI de `transacao.sh` intacta. `.claude/skills/` nao existe neste repo.
- Learnings for future iterations:
  - Sortino de snapshot esparso nao usa 252; mutar so `MIN_NAV` e TDR fraco (1 ponto cai em ZeroDivisionError no downside, nao na string).
  - Aliquota hardcoded no valor do fixture (`return 0.2`) nao mata o happy-path; TDR genuino do AC3 e inventar numero no ramo de campos ausentes.
  - GATE 6: tdr-runner/qa-reviewer nao resolvem como `subagent_type` neste harness Grok; despacho via general-purpose carregando o contrato (mesmo precedente de fh-US-007).
- Achados LOW do qa-reviewer (nao-bloqueantes, backlog): (1) `impostoPago` e `ganhoRealizado` somados em listas independentes, sem parear por linha; (2) giro=0.0 (nao a string) quando o arquivo existe so com aporte/resgate. Nenhum e X→Z.
- GATE 1: tests passing (146/146) | GATE 2: lint clean (`bash -n`; shellcheck indisponivel) | GATE 3: typecheck clean (`bash -n`, `py_compile`, `jq empty`) | GATE 4: coverage n/a (Bash/Bats/Python sem tooling ou threshold) | GATE 5: committed at 4315556 | GATE 6: guarded-impl-loop PASS (tdr: validated=yes mechanism=genuine; adherence: PASS)
---


