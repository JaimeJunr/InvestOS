.guarded-impl/
.tdr/
.tdr-evidence/
__pycache__/
*.pyc

# Workspace interno do processo de geração (PRD, loop de implementação, evidência de
# review) — nunca vai pro repo publico. O conteudo legivel vive em docs/ (ver
# docs/desenvolvimento/contribuindo.md sobre o processo /prd -> /ralph).
.ralph/

